spring:
  application:
    name: api-gateway
  profiles:
      active: prod
  cloud:
    config:
        import: ${CONFIG_SERVER_URL}
        fail-fast: true
  retry:
    initial-interval: 3000
    max-attempts: 10
    multiplier: 1.5