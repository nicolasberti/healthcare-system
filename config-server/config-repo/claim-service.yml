spring:
  application:
    name: claim-service
  datasource:
    url: jdbc:mysql://mysql:3306/claims
    username: root # Agregar AWS Secrets Manager
    password: root # Agregar AWS Secrets Manager
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
      hibernate:
        ddl-auto: none
      database-platform: org.hibernate.dialect.MySQL8Dialect
      show-sql: false

server:
  port: 8083